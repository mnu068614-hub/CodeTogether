# CodeTogether

This repository now includes a minimal runnable scaffold so contributors can start the app and validate baseline behavior.

## Available scripts

- `npm start` — runs a lightweight static server on `http://localhost:4173`
- `npm test` — runs a small smoke test that verifies required scaffold files exist
